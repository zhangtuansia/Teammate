// The prompt lives in @teammate/shared so the settings screen can show the
// same text the runtime sends, rather than a description of it that drifts.
export { buildSystemPrompt } from "@teammate/shared/system-prompt";
